
import { useState } from "react";
// import { menu } from "../../../../data/Menu"


const Menubar = () => {
    

    return (
      <>
        {/* <div className="menu-bar max-h-[80vh] px-3 pt-3 overflow-y-auto">
        {menu.map((item:any) => (
            <div key={item.id}>
              <h6 className="capitalize text-xs text-slate-300 my-2 font-light">{item.title}</h6>
              {item.listItems.map((listItem:any) => (
                <Link to={listItem.url} className={listItem.is_active ? "flex items-center font-normal text-white px-3 py-2 my-1 rounded-md bg-teal-400" : "flex items-center font-normal text-gray-500 px-3 py-2 my-1 rounded-md hover:duration-200 hover:pl-4 hover:text-teal-400" } key={listItem.id}>
                  <span className="">{listItem.title}</span>
                </Link>
              ))}
            </div>
        ))}
        </div> */}

        

      </>
    )
  }

export default Menubar
